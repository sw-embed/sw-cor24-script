Implemented command dispatch table with function pointer dispatch. Added cmd_register/cmd_find for a parallel-array command table. Extracted handlers: cmd_echo, cmd_set, cmd_exit, cmd_exists, cmd_toktest, cmd_valtest. Main loop uses cmd_find + indirect call through function pointer. Pre-expansion dispatch for exists?. Unknown command error reporting. Exit command with flag. Prompt changed to sws>. Discovered tc24r limitation: global arrays cannot use multiply expressions in size (e.g. char pool[A * B] fails, must use pre-computed #define). Function pointers stored as int in globals, cast to fn-ptr locally for indirect calls. Test suite expanded from 34 to 39 tests.