Implemented run command, pragma run-rc on, and structured $rc record. The run command currently simulates not-found for all programs (process execution not yet available on COR24). pragma only accepts 'run-rc on' — rejects off, missing args, and unknown pragmas. set_rc_record builds VAL_REC with outcome-dependent fields (run/kind always, prog for success, err/msg for failures). $rc naturally errors when accessed before pragma or before first run. Added 11 tests (50 total, all passing).