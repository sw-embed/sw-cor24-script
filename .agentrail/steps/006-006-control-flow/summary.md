Implemented control flow: block evaluation engine (eval_line/eval_block with global block stack), if/else, while with break/continue, comparison commands (eq/ne/lt/gt/le/ge), logic commands (and/or/not), incr command, reserved keyword errors. Fixed CMD_NAME_POOL_SIZE buffer overflow and switched from stack-local to global block buffers to work within tc24r's stack offset constraints.